# nixpacks.toml

[phases.setup]
nixPkgs = ['python', 'yarn', 'uwsgi]

[phases.install]
cmd = ['python -m pip install -r requirements.txt']

[phases.build]
cmds = ['yarn run build']

[start]
cmd = 'uwsgi --ini app.ini'